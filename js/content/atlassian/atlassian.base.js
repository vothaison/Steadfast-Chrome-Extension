; (function (window) {
    V.Extension = V.Extension || {};

    V.Extension.atlassianPort = chrome.runtime.connect({ name: "atlassian" });

    V.Box = V.Box || {};

    V.Box.Regexes = {

    }

}(window));


console.info("atlassian Extension is Running");





